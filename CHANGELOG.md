## 📜 چنج‌لاگ اپتیک‌نامه

### v0.1 Alpha — نمونهٔ اولیهٔ داده‌محور
**افزوده‌ها**
*   معماری داده‌محور: شیء `COURSE` به‌عنوان تنها منبع حقیقت؛ ۱۵ جلسه با مفاهیم، فرمول‌ها، نکات، نمودارها و پانوشت‌ها.
*   صفحات: داشبورد (نمره‌دهی، منابع، فصل‌ها)، جلسه، آزمایشگاه، مرجع فرمول‌ها، واژه‌نامه، راهنمای توسعه.
*   ناوبری هش‌محور، سایدبار با حلقهٔ پیشرفت، جست‌وجوی سراسری، علامت «مطالعه شده» با ذخیرهٔ لوکال.
*   ۸ ویجت تعاملی نسل اول (آینه، عدسی، شکست، یانگ، جسم سیاه، موج EM، فازور، موج ایستاده).
*   رابط تاریک با وزیرمتن؛ رندر ریاضی و نمودارها در ابتدا از CDN.

### v0.2 Alpha — تثبیت موتور رندر و پایداری
**تغییرها**
*   آفلاین‌سازی کامل کتابخانه‌ها (KaTeX، auto-render، Mermaid) — حذف وابستگی به اینترنت.
**اصلاح‌ها**
*   فرمول‌های خام `$...$`: متغیرهای داخل `<code>` که توسط auto-render نادیده گرفته می‌شدند، با `<span dir="ltr">` جایگزین شدند.
*   هنگ `RESULT_CODE_HUNG` در مرجع فرمول‌ها و واژه‌نامه: رندر سبک‌تر و تفکیک‌شده (کارت‌به‌کارت).
*   به‌هم‌ریختگی جهت متن در پنل اطلاعات ویجت‌ها (`unicode-bidi: plaintext`).
**حذف‌ها**
*   `MutationObserver` و لاگ‌های اسپمی کنسول که عامل حلقه‌های رندر بودند.

### v0.3 Alpha — بازسازی آزمایشگاه تعاملی
**اصلاح‌ها**
*   فیزیک و نمایش آینه/عدسی: محاسبهٔ صحیح `s′=sf/(s−f)` و `m=−s′/s`؛ نمایش کسری با `\frac`.
*   ویجت یانگ: رفع عدم‌تعامل اسلایدرها؛ استفاده از مقیاس نمایش ثابت (۱۵mm) تا تغییر تراکم فریزها با تغییر `d` دیده شود.
*   ویجت موج EM: افزودن محور پرسپکتیو برای میدان B و برچسب‌گذاری محورهای E، B و راستای انتشار.
*   ویجت فازور: رسم هر دو فازور از مبدأ با متوازی‌الاضلاع؛ پنل اطلاعات فقط یونیکد.
**افزوده‌ها**
*   رفع تداخل رندر در توضیح فازور با رندر دستی `katex.render` در جای‌نشان اختصاصی (خارج از سیستم auto-render).

### v0.4 Alpha — بسته‌بندی و قابل‌حمل‌سازی
**افزوده‌ها**
*   فونت وزیرمتن لوکال (`vazirmatn-local.css` با زیرمجموعه‌های arabic/latin و `unicode-range`).
*   ساختار پروژه: `Optics.html` در ریشه + `src/{css, js, fonts}`.
*   اجرای با دابل‌کلیک بدون نصب و بدون اینترنت؛ سند Word راهنما.
**تغییرها**
*   افزودن خطوط امضا/توضیح به فوترهای داشبورد و راهنما.

### v0.5 Alpha — کنترل کیفیت محتوا و برندینگ 
**اصلاح‌ها**
*   **ریشهٔ فرمول‌های دوتایی/برعکس:** غلط تایپی `scr` در مسیر `katex.min.css` → بدون CSS، هر دو لایهٔ خروجی KaTeX (متن MathML + نسخهٔ بصری) نمایش داده می‌شد؛ مسیر به `src` اصلاح شد.
*   ساعات جلسات از نام فایل‌های خام تکمیل شد: جلسهٔ ۷ (تاریخ + ساعت)، ۱۱، ۱۲ و ۱۵ (ساعت).
*   کسرهای سطری به کسر عمودی تبدیل شدند: توضیح ویجت‌های آینه/عدسی، مثال‌های عددی جلسات ۱۰ و ۱۱ (+ موارد اختیاری واژه‌نامه و جلسهٔ ۳).
**حذف‌ها**
*   بارگذاری دوبل اسکریپت‌های KaTeX/auto-render و پاس رندر سراسری `DOMContentLoaded` (قانون تک‌پاس رندر).
*   تعریف‌های تکراری `wl2rgb` (۴ → ۱).
**تغییرها**
*   «قواعد تدوین برای هوش مصنوعی» → **«منشور تدوین هادی عبدالهی»**؛ فشرده‌سازی ۸ قاعده به ۴ قاعده با ذکر پیشینهٔ پایپ‌لاین خام→مارک‌داون→اپ.
*   رندر فرمول‌ها در نتایج جست‌وجو (`katexify(res)`).

### v0.6 Alpha - جلسات جدید و تم روشن
**افزوده‌ها**
*   پوستهٔ روشن
*  افزودن جلسات 16 - 17 و 18

**اصلاح‌ها**
*  بهبود کلی و اصلاح برخی بخش ها
